commit b020a2b0ee2268287ea3608582058e97117808c0
Merge: b272517 a48208d
Author: sizzlecar <pandaathome5479@gmail.com>
Date:   Tue Mar 31 14:25:01 2026 +0800

    merge feature/lindorm-datasource into test
    
    Resolve conflicts in FCMPushService: keep resolvePushId/pushId from test,
    keep refactored FcmResultProcessor/PushUrlBuilder/FcmBatchContext from feature branch.

commit a48208ded170a01a4ca7f8ab08a88999092af37f
Author: sizzlecar <pandaathome5479@gmail.com>
Date:   Tue Mar 31 13:56:36 2026 +0800

    refactor: review fixes - implement interface, null-safe enable, extract FcmBatchContext
    
    - LindormQueryService implements UserDeviceQuery for compile-time safety
    - LindormWriteService: handle null enable with setNull instead of setObject
    - Extract FcmBatchContext to reduce FcmResultProcessor.process() parameters

diff --git a/quan-message-backend/src/main/java/com/quan/message/backend/listener/FCMPushService.java b/quan-message-backend/src/main/java/com/quan/message/backend/listener/FCMPushService.java
index 235db63..aa06a23 100644
--- a/quan-message-backend/src/main/java/com/quan/message/backend/listener/FCMPushService.java
+++ b/quan-message-backend/src/main/java/com/quan/message/backend/listener/FCMPushService.java
@@ -8,6 +8,7 @@ import com.quan.common.enums.TargetJumpTypeEnum;
 import com.quan.message.common.dto.*;
 import com.quan.message.common.response.MessageContentVo;
 import com.quan.message.service.bigdata.UserDevice;
+import com.quan.message.backend.service.FcmBatchContext;
 import com.quan.message.backend.service.FcmMessageBuilder;
 import com.quan.message.backend.service.FcmResultProcessor;
 import com.quan.message.backend.service.FirebaseAccountSelector;
@@ -163,6 +164,10 @@ public class FCMPushService {
         AtomicLong iosFcmError = new AtomicLong(0L);
         StringBuilder errorDetails = new StringBuilder();
 
+        FcmBatchContext ctx = new FcmBatchContext(tokenToDeviceId, tokenToPlatform,
+                message.getRefId(), androidSuccess, iosSuccess, androidFcmError, iosFcmError,
+                errorDetails, taskUuid, result);
+
         try {
             Map<String, UserDevice> tokenToDeviceMap = deviceMap.values().stream()
                     .filter(d -> d.getToken() != null && batchTokens.contains(d.getToken()))
@@ -273,9 +278,8 @@ public class FCMPushService {
                     successCount += batchResponse.getSuccessCount();
                     failureCount += batchResponse.getFailureCount();
 
-                    fcmResultProcessor.process(batchResponse, langTokens, tokenToDeviceId, tokenToPlatform,
-                            message.getRefId(), androidSuccess, iosSuccess, androidFcmError, iosFcmError,
-                            errorDetails, title, content, scheme, analyticsLabel, taskUuid, result);
+                    fcmResultProcessor.process(batchResponse, langTokens, ctx,
+                            title, content, scheme, analyticsLabel);
                 }
             }
         } catch (Exception e) {
diff --git a/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmBatchContext.java b/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmBatchContext.java
new file mode 100644
index 0000000..f778551
--- /dev/null
+++ b/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmBatchContext.java
@@ -0,0 +1,55 @@
+package com.quan.message.backend.service;
+
+import com.quan.message.common.dto.FcmPushResult;
+import lombok.Data;
+
+import java.util.Map;
+import java.util.concurrent.atomic.AtomicLong;
+
+/**
+ * FCM批次处理上下文
+ * 封装一次sendFcmBatch中需要在多个语言/平台分组间共享的计数器和映射
+ */
+@Data
+public class FcmBatchContext {
+
+    /** token → deviceId 映射 */
+    private final Map<String, String> tokenToDeviceId;
+    /** token → platform 映射 */
+    private final Map<String, String> tokenToPlatform;
+    /** 任务refId（用于日志） */
+    private final Long refId;
+
+    // ---- 平台维度计数器 ----
+    private final AtomicLong androidSuccess;
+    private final AtomicLong iosSuccess;
+    private final AtomicLong androidFcmError;
+    private final AtomicLong iosFcmError;
+
+    /** 错误详情（限制长度，由调用方传入） */
+    private final StringBuilder errorDetails;
+
+    /** 任务uuid */
+    private final String taskUuid;
+
+    /** 累积结果（成功/失败设备ID列表） */
+    private final FcmPushResult result;
+
+    public FcmBatchContext(Map<String, String> tokenToDeviceId, Map<String, String> tokenToPlatform,
+                           Long refId,
+                           AtomicLong androidSuccess, AtomicLong iosSuccess,
+                           AtomicLong androidFcmError, AtomicLong iosFcmError,
+                           StringBuilder errorDetails,
+                           String taskUuid, FcmPushResult result) {
+        this.tokenToDeviceId = tokenToDeviceId;
+        this.tokenToPlatform = tokenToPlatform;
+        this.refId = refId;
+        this.androidSuccess = androidSuccess;
+        this.iosSuccess = iosSuccess;
+        this.androidFcmError = androidFcmError;
+        this.iosFcmError = iosFcmError;
+        this.errorDetails = errorDetails;
+        this.taskUuid = taskUuid;
+        this.result = result;
+    }
+}
diff --git a/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmResultProcessor.java b/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmResultProcessor.java
index 5821c56..0bdd217 100644
--- a/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmResultProcessor.java
+++ b/quan-message-backend/src/main/java/com/quan/message/backend/service/FcmResultProcessor.java
@@ -7,7 +7,6 @@ import com.quan.message.common.constants.Constant;
 import com.quan.message.common.constants.PushTaskConstants;
 import com.quan.message.common.dto.FcmPushResult;
 import com.quan.message.common.dto.FcmTokenMsgDto;
-import com.quan.message.common.request.FcmTokenRequest;
 import com.quan.rocketmq.starter.config.RocketMqProducer;
 import lombok.RequiredArgsConstructor;
 import lombok.extern.slf4j.Slf4j;
@@ -17,9 +16,7 @@ import org.springframework.stereotype.Service;
 
 import java.util.ArrayList;
 import java.util.List;
-import java.util.Map;
 import java.util.Objects;
-import java.util.concurrent.atomic.AtomicLong;
 
 /**
  * FCM推送结果处理器
@@ -38,16 +35,20 @@ public class FcmResultProcessor {
 
     /**
      * 处理批次响应，更新统计计数器和 result 中的设备ID列表
+     *
+     * @param batchResponse  FCM批量发送响应
+     * @param tokens         本次发送的token列表
+     * @param ctx            批次上下文（计数器、映射、结果收集）
+     * @param title          通知标题（用于重试）
+     * @param content        通知内容（用于重试）
+     * @param scheme         跳转链接（用于重试）
+     * @param analyticsLabel 分析标签（用于重试）
      */
     public void process(BatchResponse batchResponse, List<String> tokens,
-                        Map<String, String> tokenToDeviceId, Map<String, String> tokenToPlatform,
-                        Long refId,
-                        AtomicLong androidSuccess, AtomicLong iosSuccess,
-                        AtomicLong androidFcmError, AtomicLong iosFcmError,
-                        StringBuilder errorDetails,
-                        String title, String content, String scheme, String analyticsLabel,
-                        String taskUuid, FcmPushResult result) {
+                        FcmBatchContext ctx,
+                        String title, String content, String scheme, String analyticsLabel) {
 
+        FcmPushResult result = ctx.getResult();
         List<SendResponse> responses = batchResponse.getResponses();
 
         if (result.getSuccessDeviceIds() == null) {
@@ -59,32 +60,32 @@ public class FcmResultProcessor {
 
         for (int i = 0; i < responses.size(); i++) {
             String token = tokens.get(i);
-            String deviceId = tokenToDeviceId.getOrDefault(token, "unknown");
-            String platform = tokenToPlatform.getOrDefault(token, PushTaskConstants.Platform.ANDROID);
+            String deviceId = ctx.getTokenToDeviceId().getOrDefault(token, "unknown");
+            String platform = ctx.getTokenToPlatform().getOrDefault(token, PushTaskConstants.Platform.ANDROID);
             boolean isIos = PushTaskConstants.Platform.IOS.equalsIgnoreCase(platform);
 
             if (responses.get(i).isSuccessful()) {
                 if (isIos) {
-                    iosSuccess.getAndIncrement();
+                    ctx.getIosSuccess().getAndIncrement();
                 } else {
-                    androidSuccess.getAndIncrement();
+                    ctx.getAndroidSuccess().getAndIncrement();
                 }
                 if (!"unknown".equals(deviceId) && !result.getSuccessDeviceIds().contains(deviceId)) {
                     result.getSuccessDeviceIds().add(deviceId);
                 }
-                log.info(Constant.LOG_PREFIX + " FCM推送成功, deviceId={}, platform={}", refId, deviceId, platform);
+                log.info(Constant.LOG_PREFIX + " FCM推送成功, deviceId={}, platform={}", ctx.getRefId(), deviceId, platform);
             } else {
                 com.google.firebase.messaging.FirebaseMessagingException ex = responses.get(i).getException();
                 MessagingErrorCode errorCode = ex != null ? ex.getMessagingErrorCode() : null;
                 String errorMsg = String.format(
                         "FCM发送失败, deviceId=%s, token=%s, platform=%s, 错误码=%s, 错误信息=%s",
                         deviceId, token, platform, errorCode, ex != null ? ex.getMessage() : null);
-                log.warn(Constant.LOG_PREFIX + " {}", refId, errorMsg);
+                log.warn(Constant.LOG_PREFIX + " {}", ctx.getRefId(), errorMsg);
 
                 if (isIos) {
-                    iosFcmError.getAndIncrement();
+                    ctx.getIosFcmError().getAndIncrement();
                 } else {
-                    androidFcmError.getAndIncrement();
+                    ctx.getAndroidFcmError().getAndIncrement();
                 }
                 if (!"unknown".equals(deviceId) && !result.getFailureDeviceIds().contains(deviceId)) {
                     result.getFailureDeviceIds().add(deviceId);
@@ -93,21 +94,21 @@ public class FcmResultProcessor {
                 if (Objects.equals(errorCode, MessagingErrorCode.UNREGISTERED)
                         || Objects.equals(errorCode, MessagingErrorCode.INVALID_ARGUMENT)) {
                     log.info(Constant.LOG_PREFIX + " FCM令牌无效，准备异步禁用设备: refId={}, deviceId={}, token={}, errorCode={}",
-                            refId, deviceId, token, errorCode);
+                            ctx.getRefId(), deviceId, token, errorCode);
                     sendDisableTokenAsync(token, deviceId, platform);
                 }
 
-                if (errorDetails.length() < 1000) {
-                    if (errorDetails.length() > 0) {
-                        errorDetails.append("; ");
+                if (ctx.getErrorDetails().length() < 1000) {
+                    if (ctx.getErrorDetails().length() > 0) {
+                        ctx.getErrorDetails().append("; ");
                     }
-                    errorDetails.append(errorMsg);
+                    ctx.getErrorDetails().append(errorMsg);
                 }
             }
         }
 
-        fcmRetryService.processFcmResults(batchResponse, tokens, tokenToDeviceId, tokenToPlatform,
-                refId, title, content, scheme, analyticsLabel, taskUuid);
+        fcmRetryService.processFcmResults(batchResponse, tokens, ctx.getTokenToDeviceId(), ctx.getTokenToPlatform(),
+                ctx.getRefId(), title, content, scheme, analyticsLabel, ctx.getTaskUuid());
     }
 
     @Async
diff --git a/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormQueryService.java b/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormQueryService.java
index a310c85..0e050ed 100644
--- a/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormQueryService.java
+++ b/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormQueryService.java
@@ -31,7 +31,7 @@ import java.util.List;
  * 仅当lindorm.url配置后才生效
  */
 @Slf4j
-public class LindormQueryService {
+public class LindormQueryService implements UserDeviceQuery {
 
     private final DataSource dataSource;
     private final LindormProperties properties;
diff --git a/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormWriteService.java b/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormWriteService.java
index e5504c4..e93ecc1 100644
--- a/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormWriteService.java
+++ b/quan-message-service/src/main/java/com/quan/message/service/bigdata/LindormWriteService.java
@@ -90,7 +90,11 @@ public class LindormWriteService {
             ps.setString(5, request.getLanguage());
             ps.setString(6, request.getClientVersion());
             ps.setString(7, request.getClientType());
-            ps.setObject(8, request.getEnable());
+            if (request.getEnable() != null) {
+                ps.setInt(8, request.getEnable());
+            } else {
+                ps.setNull(8, java.sql.Types.INTEGER);
+            }
             ps.setTimestamp(9, now);
             ps.setTimestamp(10, now);
             ps.executeUpdate();

commit b27251701e139dfdfd2defb58b9a278a694f8dff
Merge: 67fbb6c 220f443
Author: sizzlecar <pandaathome5479@gmail.com>
Date:   Wed Mar 25 11:47:43 2026 +0800

    Merge branch 'feat/manual-fcm-device-push' into test
