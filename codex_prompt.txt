请用更通俗易懂的语言（最好带个比喻）来解释下面这个Spring Cloud Gateway的内存泄漏问题和修复方案。

【问题背景】
内存泄漏是因为 SignFilter.java 重写了请求体。它读取POST请求体后重新编码，这会分配一个 Netty ByteBuf（内存块）。如果后面的过滤器直接拦截并返回了响应，这个重新编码的请求体就不会被处理，导致分配的内存块永远不会被释放。

【修复方案】
读取一次请求体，验证签名后，立刻手动释放内存块，只保留原始的 byte[] 数据。然后让 getBody() 每次被调用时再包装返回这些数据。

请用大白话向用户解释。
