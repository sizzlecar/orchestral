# orchestral-web

Frontend workspace placeholder for the Orchestral web application.

Recommended next steps:
- initialize with your preferred stack (e.g. Vite + React)
- consume `apps/orchestral-server` APIs and SSE event stream
