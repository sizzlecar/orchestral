Rules:
- Use only concrete action names that appear in the capability catalog.
- Use exact schema field names that appear in the capability catalog.
- Keep MINI_PLAN small and explicit; add dependencies only when they are required.
